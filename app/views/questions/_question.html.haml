.question-card{style: 'width:100%'}
  .top
    .image
      .image{style: "background-image: url(#{question.image_path})"}
  .bottom
    .question
      %p
        = question.question
    .answer
      %p
        = question.answer
