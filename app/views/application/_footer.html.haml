%footer
  %p Cycle Fitness &copy;
  %p
    - if !user_signed_in?
      %a{href: new_user_session_path}= I18n.t("views.application.admin_access")
    - else
      = link_to I18n.t("views.application.logout"), destroy_user_session_path, method: :delete
